# Variáveis de ambiente para Vercel
# Copie estas variáveis para o painel da Vercel

# Database URL do Neon
DATABASE_URL=postgresql://username:password@host/database?sslmode=require

# Configurações opcionais
NODE_ENV=production